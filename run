cd ~/proyecto

while read -r pid
do
  kill -9 $pid
done < run.pid

git pull

if [ $? -eq 0 ]; then
  webpack -w &
  echo $! > run.pid
  npm run sfs &
  echo $! >> run.pid
  npm run bff &
  echo $! >> run.pid
fi
